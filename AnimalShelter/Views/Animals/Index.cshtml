@using AnimalShelter.Models;
@model List<AnimalShelter.Models.Animal>;
@{
  Layout = "_Layout";
}
<nav class="navbar navbar-light bg-light">
  <li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
      aria-haspopup="true" aria-expanded="false">
      Sort
    </a>
    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
      <li> @Html.ActionLink("Breed", "Index", new {sortOrder = ViewBag.BreedSortParm})</li>
      <li> @Html.ActionLink("Gender", "Index", new {sortOrder = ViewBag.GenderSortParm})</li>
    </ul>
  </li>
</nav>
<h1>Animals<h1>


    @if (@Model.Count == 0)
    {
      <p>There are no animals listed!</p>
    }
    @* drop down -> name, dateofadmit, type, breed *@
    @* put the selection in line 13 after model. *@

    @* @Html.DropDownList(
      @foreach (var option in ViewData["options"] as List<string>)
      {
      <li>new SelectListItem { Text = option, Value = option}</li>
      }
      ) *@
    @* @Html.DropDownListFor(model => model.Type, new List<SelectListItem>
      { *@




    @* <div class="dropdown show">
      <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown"
      aria-haspopup="true" aria-expanded="false">
      Dropdown link
      </a>

      <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
      <a class="dropdown-item"> @Html.ActionLink("Name", "Index", new {sortOrder = ViewBag.NameSortParm})<<a>
      <div class="dropdown-item"> @Html.ActionLink("Date", "Index", new {sortOrder =
      ViewBag.DateSortParm})</div>
      <a> @Html.ActionLink("Breed", "Index", new {sortOrder = ViewBag.BreedSortParm})</a>
      <a> @Html.ActionLink("Gender", "Index", new {sortOrder = ViewBag.GenderSortParm})</a>
      <a> @Html.ActionLink("Type", "Index", new {sortOrder = ViewBag.TypeSortParm})</a>
      </div>
      </div> *@


    @foreach (Animal animal in Model)
    {
      <li>@Html.ActionLink($"{animal.Name}", "Details", new {id = animal.AnimalId})</li>
    }
    </ul>

    <p>@Html.ActionLink("Add new Animal", "Create")</p>
